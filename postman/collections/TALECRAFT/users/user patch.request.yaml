$kind: http-request
url: http://127.0.0.1:5000/api/user
method: PATCH
body:
  type: json
  content: |-
    {   
        "is_active": true
    }
auth:
  type: bearer
  credentials:
    token: "{{access_token}}"
order: 4000
