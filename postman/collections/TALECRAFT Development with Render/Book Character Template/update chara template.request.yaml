$kind: http-request
name: update chara template
url: "{{dev_url}}/api/character-templates/17"
method: PATCH
body:
  type: json
  content: |-
    {
        "role": "pet"
    }
auth:
  type: bearer
  credentials:
    token: "{{access_token}}"
order: 4000
